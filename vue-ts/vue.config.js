module.exports = {
  // // 入口
  // entry: "",

  // // 出口
  // output: "",

  // // 简单的配置方式
  // configureWebpack: config => {
  //   if (process.env.NODE_ENV === 'production') {
  //     // 为生产环境修改配置
  //   } else {
  //     // 为开发环境修改配置
  //   }
  // },

  // // 链式操作：更细粒度的控制内部配置，可以定义具名的 loader 规则和具名插件
  // chainWebpack: config => {

  // }
}
